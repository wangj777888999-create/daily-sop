# 智能工作台 — 待办总清单

> 整理日期：2026-05-12  
> 所有 SOP 相关内容已废弃，不在此列。

---

## 一、Bug 修复（代码审查遗留）

### H1 · fcntl Windows 兼容
- **文件**：`backend/artifacts.py`（`backend/sops/storage.py` 已有 shim，但 artifacts.py 直接 `import fcntl`）
- **改法**：`try: import fcntl / except ImportError: fcntl = None`，写操作时判断 fcntl 是否为 None

### H3 · .gitignore 补充
- **文件**：`.gitignore`
- **加入**：`data/uploads/`、`data/artifacts/`、`data/samples/`

### H6 · 裸 except 吞异常
- **文件**：`backend/api/routes.py:55`
- **改法**：`except: params = {}` → `except (json.JSONDecodeError, KeyError, ValueError): params = {}`

### M2 · 执行响应适配器双格式
- **文件**：`src/services/sopApi.ts:20-30`
- **问题**：`/execute` 返回 `{execution_id, status}`，`/execute/{id}/status` 返回完整结构，`adaptExecutionResponse()` 未统一处理，`sop_id` 在初始响应后为 `undefined`
- **改法**：适配器显式处理两种 schema，或统一后端两个接口的返回结构

### M5 · execution_logs.json 脏数据
- **文件**：`data/execution_logs.json`
- **问题**：`input_files` 中有字符串 `"null"`
- **改法**：手动清理；后端上传成功后才追加路径（`backend/api/routes.py` 执行逻辑）

---

## 二、知识库 RAG 重建

> 后端已有旧向量方案代码（`backend/knowledge/`），需按 BM25 新方案重写。  
> 规划文档：`docs/规划/2026-05-11-rag-knowledge-base-plan.md`

### 后端重写

| 文件 | 操作 | 说明 |
|---|---|---|
| `backend/knowledge/indexer.py` | **新建**（替换 embedder.py） | `BM25Index` 类：jieba 分词 + `rank_bm25.BM25Okapi`，内存常驻，启动时从 JSON 重建 |
| `backend/knowledge/embedder.py` | **删除** | 旧向量方案，废弃 |
| `backend/knowledge/vector_store.py` | **删除** | ChromaDB 封装，废弃 |
| `backend/knowledge/storage.py` | **新增函数** | `save_chunks(doc_id, chunks)`、`load_all_chunks() -> list`、`delete_doc_chunks(doc_id)` |
| `backend/knowledge/rag.py` | **重写** | `RAGPipeline.__init__` 接收 `BM25Index`，`generate()` 移除 `api_key` 参数 |
| `backend/knowledge/generator.py` | **修改** | 函数签名去掉 `api_key` 参数，改为 `os.environ["ANTHROPIC_API_KEY"]` |
| `backend/main.py` | **修改** | startup 改为加载 chunks + 重建 BM25 索引，移除 EmbeddingService 加载 |
| `backend/requirements.txt` | **修改** | 删除 chromadb/text2vec/torch/transformers/sentence-transformers；新增 `rank-bm25>=0.2.2` |

**upload 新流程**：解析 → 分块 → `storage.save_chunks()` → `bm25_index.add_chunks()` → 返回  
**search 新流程**：`bm25_index.search(query, top_k, doc_ids)` → 补全元数据 → 返回  
**delete 新流程**：删文件 + `storage.delete_doc_chunks()` + `bm25_index.remove_doc()` + 删元数据

### 前端对接（19 处 hook）

> 详细对照表见 `docs/规划/2026-05-11-rag-knowledge-base-plan.md` 第"前端现有钩子对齐清单"节

**KnowledgePage.vue**（主要工作）：
- `folders`/`files`/`searchResults` 空数组 → 接入 `src/stores/knowledge.ts`
- 上传按钮 → `UploadDialog.vue`
- 搜索框 → `store.search(query)`
- "语义"/"关键词" chip → 只保留"关键词"（BM25 不区分语义/关键词）
- 搜索结果相关度 `r.match` → `(r.score * 100).toFixed(0) + '%'`

**PolicyPage.vue**：
- "参考资料"卡片 → 嵌入知识库搜索 + 选中文档列表
- "AI润色" → 调用 `POST /api/knowledge/generate`，`style="polish"`

**其他页面**：
- `HomePage.vue`："知识库文档" MetricCard → `store.documentCount`
- `AnalyticsPage.vue`："知识库"按钮 → 文档选择器弹窗

---

## 三、工具箱三大工具建设

> 规划文档：`docs/规划/2026-05-12-工具箱三大工具建设规划.md`  
> 源脚本：`samples/教练签到分析最终版.py`、`samples/campus_monthly_analysis.py`、`samples/offcampus_monthly_analysis.py`  
> 参考数据：`data/samples/`

### 前置工作（三个工具共用）

1. **`data/config.json` 初始化**：创建文件，填入以下结构（`部门划分.xlsx` 已在 `data/samples/`）
```json
{
  "tools": {
    "department_mapping": "data/samples/部门划分.xlsx",
    "course_types": "data/samples/26年春课程类型.xlsx",
    "offcampus_coaches": ["邓文凯", "唐崇尧", "王峥", "卞梦男", "苏长锴", "甘洪宇", "何苗", "邱毅俊", "邵志龙", "钱威", "徐思澳", "徐子杰", "许佳晴"],
    "outside_keywords": ["农都", "西溪", "滨江"],
    "target_course_types": ["兴趣班", "校队", "梯队"],
    "exclude_schools": ["乒乓球"]
  }
}
```

2. **`backend/tools/` 模块骨架**：
```
backend/tools/
  __init__.py
  db.py              # SQLite 操作层（data/tools.db）
  models.py          # Pydantic 模型
  daily_checkin.py   # 工具一
  campus_monthly.py  # 工具二
  offcampus_monthly.py # 工具三
backend/api/tool_routes.py  # 新增路由，挂载到 main.py
```

3. **SQLite 建表**（见规划文档第四节）：`daily_checkin` 表 + `checkin_batches` 表 + 两个索引

### 工具一：每日教练签到分析（先做）

- **后端**：移植 `samples/教练签到分析最终版.py`，改动点：
  - `pd.read_excel(path)` → 接收 `bytes` 参数
  - 新增 `save_to_sqlite(df, batch_info)`，利用 `INSERT OR IGNORE` 去重
  - `export_with_formatting()` → 返回 `bytes` 而非写文件
- **API**：4 个端点（preview/process/download/history，见规划文档 5.2 节）
- **前端**：`src/ui/pages/toolbox/DailyCheckin.vue`（新建），注册到 `src/data/tools.ts` + 路由

### 工具二：校内月度分析（依赖工具一写入的 SQLite 数据）

- **后端**：移植 `samples/campus_monthly_analysis.py`，签到数据改从 SQLite 读取
- **API**：4 个端点（available-months/preview/analyze/download，见规划文档 6.2 节）
- **前端**：`src/ui/pages/toolbox/CampusMonthly.vue`（新建）

### 工具三：校外月度分析（独立，不依赖前两个）

- **后端**：移植 `samples/offcampus_monthly_analysis.py`，12 个子步骤（见规划文档 7.3 节）
- **注意**：首次运行无上月数据，环比字段返回 `null`，前端需处理这种状态
- **API**：4 个端点（preview/analyze/last-month/download，见规划文档 7.2 节）
- **前端**：`src/ui/pages/toolbox/OffcampusMonthly.vue`（新建）

---

## 四、文件结构收尾

> 规划文档：`docs/规划/2026-05-12-文件结构整理计划.md` 方案 B

| 操作 | 说明 |
|---|---|
| `src/ui/pages/tools/MonthlyAnalysis.vue` → `src/ui/pages/toolbox/MonthlyAnalysis.vue` | 合并 tools/ 目录到 toolbox/，删除空目录 |
| `src/router/index.ts` | 更新 MonthlyAnalysis 路由 component 引用 |
| `src/ui/pages/sop/` | 确认为死代码后可删除（6 个文件：SOPPage/SOPList/SOPCreate/SOPImport/SOPExecute/SemanticAnnotation） |
| `src/services/sopApi.ts`、`src/types/sop.ts` | 同上，确认无 import 后删除 |

---

## 五、UI 优化（低优先级）

> 规划文档：`docs/规划/2026-04-26-ui-design-review.md` Phase 3

- **字号魔法值统一**：全局搜索替换约 100+ 处
  - `text-[11px]` → `text-label`
  - `text-[12px]` → `text-helper`
  - `text-[13px]` → `text-body`
  - `text-[10px]` → `text-micro`

---

## 执行顺序建议

```
① Bug 修复（H1/H3/H6，半天）
      ↓
② 知识库后端重建（indexer.py + storage.py + rag.py + generator.py，1-2天）
      ↓
③ 工具一：每日签到（后端 + 前端，2-3天）
      ↓
④ 知识库前端对接（KnowledgePage 19处hook，1-2天）
      ↓
⑤ 工具二：校内月度 / 工具三：校外月度（各2天，可并行）
      ↓
⑥ 文件结构收尾 + UI Phase 3（各半天）
```
