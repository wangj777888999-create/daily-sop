# 智能工作台 — 待办总清单

> 整理日期：2026-05-12 | 最后更新：2026-05-12（对照代码实际状态修订）
> 所有 SOP 相关内容已废弃，不在此列。

---

## 一、Bug 修复（代码审查遗留）

### ✅ H1 · fcntl Windows 兼容 — 已修复
`backend/artifacts.py` 已改为 `try: import fcntl / except ImportError: fcntl = None`，写操作前有 `if fcntl:` 判断。

### ✅ H3 · .gitignore 补充 — 已修复
`.gitignore` 已包含 `data/uploads/`、`data/artifacts/`、`data/samples/`、`data/checkin_output/`、`data/monthly_output/`、`data/consolidation_output/`、`data/tools.db`。

### ✅ H6 · 裸 except 吞异常 — 已修复
`backend/api/routes.py:55` 已改为 `except (json.JSONDecodeError, KeyError, ValueError): params = {}`。

### ~~M2 · 执行响应适配器双格式~~ — 无需修复
`sopApi.ts` 仅被 SOP 废弃页面引用，SOP 功能已下线，不再维护此文件。

### ~~M5 · execution_logs.json 脏数据~~ — 无需修复
SOP 执行流程已废弃，该文件不再写入新数据。

---

## 二、知识库 RAG

> 规划文档：`docs/规划/2026-05-11-rag-knowledge-base-plan.md`

### ✅ 后端 BM25 方案 — 已完成

| 文件 | 状态 |
|---|---|
| `backend/knowledge/indexer.py` | ✅ BM25Index 类，jieba 分词，内存常驻 |
| `backend/knowledge/storage.py` | ✅ save_chunks / load_all_chunks / delete_doc_chunks 已实现 |
| `backend/knowledge/rag.py` | ✅ RAGPipeline，BM25 检索管线 |
| `backend/knowledge/generator.py` | ✅ 读 ANTHROPIC_API_KEY env var，无前端传参 |
| `backend/knowledge/parser.py` | ✅ 支持 PDF/DOCX/XLSX/TXT/MD |
| `backend/knowledge/chunker.py` | ✅ 中文文档分块 |
| `backend/knowledge/models.py` | ✅ Pydantic 模型 |
| `backend/api/knowledge_routes.py` | ✅ 全部端点：documents/search/generate/folders/tags/reparse |
| `backend/main.py` | ✅ lifespan 启动时加载 chunks + 重建 BM25 |
| `embedder.py` / `vector_store.py` | ✅ 旧向量方案文件已不存在（已清理） |

### ✅ 前端知识库 — 已完成

| 文件 | 状态 |
|---|---|
| `src/types/knowledge.ts` | ✅ KB 专用类型 |
| `src/services/knowledgeApi.ts` | ✅ API 调用层（139行） |
| `src/stores/knowledge.ts` | ✅ Pinia store，documents/folders/tags/search/upload（126行） |
| `src/ui/components/knowledge/DocumentCard.vue` | ✅ 已实现 |
| `src/ui/components/knowledge/UploadDialog.vue` | ✅ 已实现 |
| `src/ui/components/knowledge/SearchResultCard.vue` | ✅ 已实现 |
| `src/ui/components/knowledge/DocumentPreview.vue` | ✅ 已实现 |
| `src/ui/pages/knowledge/KnowledgePage.vue` | ✅ 接入 store，284行，19处钩子已对接 |

### 待办：其他页面集成（低优先级）

- `PolicyPage.vue`："参考资料"卡片 → 嵌入 KB 搜索 + 选中文档列表；"AI润色" → 调用 `/generate`
- `HomePage.vue`："知识库文档" MetricCard → `store.documentCount`
- `AnalyticsPage.vue`："知识库"按钮 → 文档选择器弹窗

---

## 三、工具箱

> 规划文档：`docs/规划/2026-05-12-工具箱三大工具建设规划.md`

### ✅ 工具一：每日教练签到分析 — 已完成

- 后端：`backend/tools/daily_checkin.py`（216行），`backend/api/tool_routes.py` preview/process/download/history 端点
- 前端：`src/ui/pages/tools/DailyCheckin.vue`（304行）
- SQLite：`daily_checkin` 表 + `checkin_batches` 表已建

### ✅ 工具二（额外）：签到数据整合 — 已完成

规划外新增工具，月度数据校对/补签/确认流程：
- 后端：`backend/tools/checkin_consolidation.py`（104行），load/save/confirm/unconfirm/download 端点
- 前端：`src/ui/pages/tools/CheckinConsolidation.vue`（496行，含筛选/备注/状态体系）
- 数据流：整合确认后的数据作为校内月度分析的优先数据源

### ✅ 工具三：校内月度分析 — 已完成

- 后端：`backend/tools/campus_monthly.py`（361行），preview/analyze/download/history 端点
- 前端：`src/ui/pages/tools/CampusMonthly.vue`（308行）
- 从 SQLite 签到数据（优先整合版）读取，支持财务明细/课程类型/退款补充上传

### ⬜ 工具四：校外月度分析 — 未开始

- 后端：需新建 `backend/tools/offcampus_monthly.py`，移植 `samples/offcampus_monthly_analysis.py`，12 个子步骤
- API：4 个端点（preview/analyze/last-month/download，见规划文档 7.2 节）
- 前端：`src/ui/pages/tools/OffcampusMonthly.vue` 当前仅 16 行占位页，需完整实现
- 注意：首次运行无上月数据，环比字段返回 `null`，前端需处理

---

## 四、文件结构收尾

| 操作 | 状态 | 说明 |
|---|---|---|
| `src/ui/pages/tools/` 合并到 `toolbox/` | ⬜ 待做 | 工具页面目前在 `tools/` 而非 `toolbox/`，路由引用也需同步更新 |
| `src/ui/pages/sop/` 删除 | ⬜ 待做 | 6 个文件（SOPPage/SOPList/SOPCreate/SOPImport/SOPExecute/SemanticAnnotation）确认无外部引用，可删 |
| `src/ui/components/sop/` 删除 | ⬜ 待做 | `ExecutionConfig.vue` 仅被 SOP 页面引用 |
| `src/services/sopApi.ts` 删除 | ⬜ 待做 | 仅被 SOP 页面引用，SOP 废弃后可删 |
| `src/types/sop.ts` 删除 | ⬜ 待做 | 同上 |

---

## 五、UI 优化（低优先级）

> 规划文档：`docs/规划/2026-04-26-ui-design-review.md` Phase 3

- **字号魔法值统一**：全局约 100+ 处
  - `text-[11px]` → `text-label`
  - `text-[12px]` → `text-helper`
  - `text-[13px]` → `text-body`
  - `text-[10px]` → `text-micro`

---

## 当前剩余工作（按优先级）

```
① 校外月度分析（后端 offcampus_monthly.py + 前端 OffcampusMonthly.vue，2-3天）
      ↓
② 知识库其他页面集成（PolicyPage / HomePage / AnalyticsPage，1天）
      ↓
③ 文件结构收尾（删 SOP 死代码 + tools/ 目录合并，半天）
      ↓
④ UI 字号统一（按需，低优先级）
```
